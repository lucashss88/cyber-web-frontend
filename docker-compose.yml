services:
  frontend:
    build: .
    ports:
      - "5173:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    external: true
    name: cyber-web-back_app-network